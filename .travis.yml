sudo: required

services:
  - docker

language: node_js

node_js:
  - node

branches:
  only:
    - master
    - deployment

script:
  - npm i
  - openssl aes-256-cbc -K $encrypted_584f93c26d12_key -iv $encrypted_584f93c26d12_iv -in ./devops/secrets.tar.enc -out secrets.tar -d
  - tar xvf ./secrets.tar
  - npm run build
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
  - docker build -t $DOCKER_USERNAME/heticmadu-api-app:latest -f ./devops/docker/Dockerfile ./
  - docker push $DOCKER_USERNAME/heticmadu-api-app:latest
  - ssh-keyscan -H $EC2_URL
  - chmod -R 600 ./private_key
  - eval $(ssh-agent)
  - ssh-add ./private_key
  - scp -o StrictHostKeyChecking=no ./devops/docker/docker-compose.yml ubuntu@$EC2_URL:/home/ubuntu/docker-compose.api.yml
  - scp -o StrictHostKeyChecking=no ./.env ubuntu@$EC2_URL:/home/ubuntu/.env
  - ssh -i ./private_key ubuntu@$EC2_URL 'bash -s' < ./devops/deploy.sh