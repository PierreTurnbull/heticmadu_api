version: '3.5'

services:
  api-app:
    env_file:
      - .env
    image: {{ docker_user }}/heticmadu-api-app:latest
    container_name: heticmadu-api-app
    ports:
      - 4000:4000

volumes:
  db-data: {}